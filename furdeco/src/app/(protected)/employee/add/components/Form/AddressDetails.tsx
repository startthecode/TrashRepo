"use client";

import { childFormProps } from "@/app/types/CreateEmployeeForm";


const AddressDetails: React.FC<childFormProps> = ({
  register,
  control,
  error,
}) => {
  return <div>Address Details</div>;
};

export default AddressDetails;
