"use client";

import { childFormProps } from "@/app/types/CreateEmployeeForm";


const PayslipAccount: React.FC<childFormProps> = ({
  register,
  control,
  error,
}) => {
  return <div>Payslip Account</div>;
};

export default PayslipAccount;
